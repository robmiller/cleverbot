<% if @articles.empty? %>
	No articles matched that query.
<% else %>
	The following articles matched that query:

	<% @articles.each do |article| %>
		<%= article.title %> - <%= article.url(@base_url) -%>
	<% end %>
<% end %>
